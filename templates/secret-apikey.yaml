{{- if .Values.vllm.apiKey }}
apiVersion: v1
kind: Secret
metadata:
  name: {{ include "rhaii.fullname" . }}-api-key
  labels:
    {{- include "rhaii.labels" . | nindent 4 }}
type: Opaque
stringData:
  api-key: {{ .Values.vllm.apiKey | quote }}
{{- end }}
